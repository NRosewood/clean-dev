mixin header()
  +b.HEADER.header&attributes(attributes)
    .container
      +logo
      +select-block
      +navigation
      +e.contacts
        a(href="tel:71111111111")
          +icon('phone')
          span 71111111111
        button(data-open="popup-form")
          +icon('phone')
          span Перезвоните мне
        .burger
